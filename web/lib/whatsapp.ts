export async function sendWhatsApp(to:string, message:string){ const token=process.env.NEXT_PUBLIC_WHATSAPP_TOKEN; const phoneId=process.env.NEXT_PUBLIC_WHATSAPP_PHONE_ID; if(!token||!phoneId){ console.log(`[WHATSAPP MOCK] To ${to}: ${message}`); return {mocked:true}; } const res=await fetch(`https://graph.facebook.com/v20.0/${phoneId}/messages`, {method:'POST', headers:{Authorization:`Bearer ${token}`, 'Content-Type':'application/json'}, body: JSON.stringify({messaging_product:'whatsapp', to, type:'text', text:{body:message}})}); return res.json(); } export function bookingConfirmationTemplate(g:string,r:string,c:string){ return `Hello ${g}, your HOM booking Room ${r} from ${c} confirmed. — HOM Hospitality Operations Manager`; } export function payslipTemplate(n:string, net:number){ return `Hi ${n}, Net ₦${net.toLocaleString()} (PAYE 7% + Pension 8% deducted). Sent via HOM.`; }