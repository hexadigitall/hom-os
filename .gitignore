node_modules
.next
.env.local
.flutter-plugins
.dart_tool
/build
*.keystore
*.jks
*.apk
*.aab

# Android local config (paths differ per machine)
mobile/android/local.properties
mobile/android/key.properties
mobile/android/app/upload-keystore.jks

# MSIX signing certificate (restored from GitHub Secrets in CI)
mobile/windows/hom-sign.pfx

# Android build artifacts (Gradle 9.x puts outputs in project subdir)
mobile/android/build/
mobile/android/app/build/
.vercel
.env*
